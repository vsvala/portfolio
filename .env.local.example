# Required — app will not start without these
SESSION_SECRET=
ADMIN_PASSWORD=
TURSO_URL=
TURSO_AUTH_TOKEN=

# Required only for /api/protected-doc (password-gated certificates in private-documents/)
CERTIFICATE_PASSWORD=

# Optional — contact form stays hidden without these
RESEND_API_KEY=
CONTACT_EMAIL=
